const swiper = new Swiper('.swiper', {
    // Optional parameters
    loop: true,

    // Navigation arrows
    navigation: {
        nextEl: '.body-about-block__aroow.swiper-button-next',
        prevEl: '.body-about-block__aroow.swiper-button-prev',
    },
});
